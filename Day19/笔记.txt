今日内容：
    一、叠加多个装饰器的加载、运行分析（了解）
    # @deco1 # index = deco1(deco2.wrapper的内存地址)
    # @deco2 # deco2.wrapper的内存地址=deco2(deco3.wrappeer的内存地址)
    # @deco3 # deco3.wrapper的内存地址=deco3(index)
    # def index():
    #     pass

    二、生成器的高级玩法之yield挂起函数：yield的表达式形式
    x = yield 返回值

    三、三元表达式

    四、生成式
        列表生成式
        字典生成式
        生成器表达式

    五、函数的递归调用

    六、二分法
